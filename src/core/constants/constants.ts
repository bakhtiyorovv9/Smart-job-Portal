export enum UserRole {
  ADMIN = 'admin',
  COMPANY = 'company',
  CANDIDATE = 'candidate',
}

export enum ApplicationStatus {
  PENDING = 'pending',
  REVIEWED = 'reviewed',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}